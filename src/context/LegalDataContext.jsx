import React,{createContext,useContext,useEffect,useState}from 'react';const LegalDataContext=createContext();export function LegalDataProvider({children}){const[data,setData]=useState(null);useEffect(()=>{const cached=sessionStorage.getItem('site_sections');if(cached){try{setData(JSON.parse(cached).data);return}catch(e){}}fetch('/api/query',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'get_all_site_sections'}),credentials:'include'}).then(r=>r.json()).then(j=>{setData(j.data);sessionStorage.setItem('site_sections',JSON.stringify({data:j.data,expiresAt:Date.now()+36e5}))}).catch(()=>{})},[]);return(<LegalDataContext.Provider value={data || {}}>{children}</LegalDataContext.Provider>)}export function useLegalData(){return useContext(LegalDataContext)}
